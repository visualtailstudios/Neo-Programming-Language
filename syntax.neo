using .sysNeo:

var, name, 'charlie':
var, age, '15':
    var, test, 'false':

push, 'this is your name':
push, *name:

push, 'this is your age':
push, *age:

> if, *test...
    ^clear:
< else...
    /push, 'the console will not be cleared':
    /input:
    /var, group, 'true':

> if, *group...
    ^clear:
    ^push, 'group was cleared':
< else...
    /push, *age:


